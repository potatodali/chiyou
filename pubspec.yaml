name: chiyou
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # Firebase 관련 의존성 최신 버전으로 업데이트
  firebase_core: ^3.8.0
  firebase_auth: ^5.3.3
  cloud_firestore: ^5.5.0
  provider: ^6.1.0 # 호환 가능한 안정 버전

  # UI 개선 관련 의존성 (선택사항)
  flutter_spinkit: ^5.2.0 # 로딩 스피너 효과

  # Table Calendar 추가
  table_calendar: ^3.0.9

    # HTTP 패키지 추가
  http: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # 필요한 이미지 자원 설정
  assets:
    - assets/images/logo_me.png
    # 여기에 필요한 이미지 파일 경로 추가
